<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= publication ? 'Edit' : 'Add' %> Publication - Admin Panel</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/lucide@latest"></script>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style type="text/tailwindcss">
    body { font-family: 'Plus Jakarta Sans', sans-serif; background: #0a0a0a; color: white; }
    .sidebar-link { @apply flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-white hover:bg-white/5 transition-all; }
    .sidebar-link.active { @apply bg-teal-500/10 text-teal-400; }
    input, textarea, select { @apply w-full bg-black/20 border border-white/10 rounded-lg px-4 py-3 focus:outline-none focus:border-teal-500 transition-colors text-white; }
    label { @apply block text-sm text-gray-400 mb-1; }
  </style>
</head>
<body class="flex min-h-screen">
  <%- include('../partials/sidebar', { path: '/admin/publications' }) %>
  <main class="flex-1 ml-64 p-8">
    <%- include('../partials/header', { title: publication ? 'Edit Publication' : 'Add Publication' }) %>
    <div class="max-w-2xl mx-auto">
      <div class="bg-white/5 border border-white/10 rounded-2xl p-8">
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="mb-4 p-3 rounded-lg bg-red-500/10 border border-red-500/20 text-red-400"><%= error %></div>
        <% } %>
        <form action="/admin/publications<%= publication ? '/edit/' + publication.id : '/create' %>" method="POST" class="space-y-6">
          <div>
            <label>Title</label>
            <input type="text" name="title" value="<%= publication?.title || '' %>" required />
          </div>
          <div class="grid grid-cols-2 gap-4">
            <div>
              <label>Outlet/Publisher</label>
              <input type="text" name="outlet" value="<%= publication?.outlet || '' %>" required />
            </div>
            <div>
              <label>Date</label>
              <input type="text" name="date" placeholder="2025-08-01" value="<%= publication?.date || '' %>" required />
            </div>
          </div>
          <div>
            <label>URL</label>
            <input type="url" name="url" value="<%= publication?.url || '' %>" />
          </div>
          <div class="grid grid-cols-2 gap-4">
            <div>
              <label>Image URL</label>
              <input type="text" name="imageUrl" value="<%= publication?.imageUrl || '' %>" />
            </div>
            <div>
              <label>Tags (comma separated)</label>
              <input type="text" name="tags" placeholder="web, uiux" value="<%= publication?.tags || '' %>" />
            </div>
          </div>
          <div>
            <label>Description</label>
            <textarea name="description" rows="4" placeholder="Deskripsi singkat"><%= publication?.description || '' %></textarea>
          </div>
          <div class="flex gap-3">
            <button type="submit" class="flex-1 bg-teal-500 hover:bg-teal-400 text-black font-bold py-3 rounded-lg transition-colors">
              <%= publication ? 'Update Publication' : 'Add Publication' %>
            </button>
            <a href="/admin/publications" class="flex-1 bg-white/10 hover:bg-white/20 text-white font-bold py-3 rounded-lg text-center transition-colors">Cancel</a>
          </div>
        </form>
      </div>
    </div>
  </main>
</body>
</html>
