<!DOCTYPE html>
<html lang="id">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= award ? 'Edit' : 'Tambah' %> Award - Admin Panel
  </title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/lucide@latest"></script>
  <link
    href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap"
    rel="stylesheet">
  <style type="text/tailwindcss">
    body { font-family: 'Plus Jakarta Sans', sans-serif; background: #0a0a0a; color: white; }
      .sidebar-link { @apply flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-white hover:bg-white/5 transition-all; }
      .sidebar-link.active { @apply bg-teal-500/10 text-teal-400; }
  </style>
</head>

<body class="flex min-h-screen">
  <%- include('../partials/sidebar', { path: '/admin/awards' }) %>
    <main class="flex-1 ml-64 p-8">
      <%- include('../partials/header', { title: (award ? 'Edit' : 'Add New' ) + ' Award' }) %>

        <% if (error) { %>
          <div class="mb-6 p-4 bg-red-500/10 border border-red-500/20 rounded-xl text-red-400 flex items-center gap-3">
            <i data-lucide="alert-circle" class="w-5 h-5"></i>
            <span>
              <%= error %>
            </span>
          </div>
          <% } %>

            <form action="<%= award ? '/admin/awards/edit/' + award.id : '/admin/awards/create' %>" method="POST"
              enctype="multipart/form-data" class="max-w-xl space-y-4">
              <div>
                <label class="block text-sm text-gray-400 mb-1">Judul</label>
                <input type="text" name="title" value="<%= award?.title || '' %>"
                  class="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white" required>
              </div>
              <div>
                <label class="block text-sm text-gray-400 mb-1">Penyelenggara</label>
                <input type="text" name="organizer" value="<%= award?.organizer || '' %>"
                  class="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white" required>
              </div>
              <div>
                <label class="block text-sm text-gray-400 mb-1">Tanggal</label>
                <input type="text" name="date" value="<%= award?.date || '' %>"
                  placeholder="YYYY-MM-DD atau Bulan Tahun"
                  class="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white" required>
              </div>
              <div>
                <label class="block text-sm text-gray-400 mb-1">Deskripsi</label>
                <textarea name="description" rows="4"
                  class="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white"><%= award?.description || '' %></textarea>
              </div>
              <div class="grid md:grid-cols-2 gap-4">
                <div>
                  <label class="block text-sm text-gray-400 mb-1">Kategori</label>
                  <select name="category"
                    class="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white">
                    <option value="">Pilih kategori</option>
                    <option value="teknologi" <%=award?.category==='teknologi' ? 'selected' : '' %>>Teknologi</option>
                    <option value="bahasa" <%=award?.category==='bahasa' ? 'selected' : '' %>>Bahasa</option>
                    <option value="soft-skill" <%=award?.category==='soft-skill' ? 'selected' : '' %>>Soft Skill
                    </option>
                  </select>
                </div>
                <div>
                  <label class="block text-sm text-gray-400 mb-1">Credential ID</label>
                  <input type="text" name="credentialId" value="<%= award?.credentialId || '' %>"
                    class="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white">
                </div>
              </div>
              <div class="space-y-4">
                <label class="text-sm font-medium text-gray-400">Certificate Image / PDF</label>

                <div class="bg-black/40 border border-white/5 rounded-2xl p-6 backdrop-blur-sm">
                  <% const isCertUrl=award && award.certificateUrl && !award.certificateUrl.startsWith('/uploads/'); %>

                    <!-- Modern Segmented Toggle -->
                    <div class="inline-flex p-1 bg-white/5 rounded-xl mb-6 relative w-full sm:w-auto">
                      <div id="toggleHighlight"
                        class="absolute h-[calc(100%-8px)] transition-all duration-300 ease-out bg-teal-500 rounded-lg shadow-[0_0_20px_rgba(20,184,166,0.3)] pointer-events-none">
                      </div>

                      <label class="relative flex-1 sm:w-32 py-2 text-center cursor-pointer z-10">
                        <input type="radio" name="imageSource" value="file" <%=!isCertUrl ? 'checked' : '' %>
                        class="sr-only" onchange="updateToggleUI('file')">
                        <span
                          class="text-sm font-semibold transition-colors duration-300 <%= !isCertUrl ? 'text-black' : 'text-gray-400' %>">Upload
                          File</span>
                      </label>

                      <label class="relative flex-1 sm:w-32 py-2 text-center cursor-pointer z-10">
                        <input type="radio" name="imageSource" value="url" <%=isCertUrl ? 'checked' : '' %>
                        class="sr-only" onchange="updateToggleUI('url')">
                        <span
                          class="text-sm font-semibold transition-colors duration-300 <%= isCertUrl ? 'text-black' : 'text-gray-400' %>">URL
                          Link</span>
                      </label>
                    </div>

                    <div class="space-y-4">
                      <div id="fileInputContainer" class="hidden animate-in fade-in slide-in-from-top-2 duration-300">
                        <div
                          class="border-2 border-dashed border-white/10 hover:border-teal-500/30 rounded-xl p-8 text-center transition-colors group">
                          <input type="file" name="certificate" id="certInput" accept="image/*,.pdf" class="sr-only">
                          <label for="certInput" class="cursor-pointer">
                            <div
                              class="bg-teal-500/10 w-10 h-10 rounded-full flex items-center justify-center mx-auto mb-3 group-hover:scale-110 transition-transform">
                              <i data-lucide="upload" class="w-5 h-5 text-teal-400"></i>
                            </div>
                            <p class="text-xs text-gray-300 font-medium">Upload Certificate</p>
                            <p class="text-[10px] text-gray-500 mt-1">Images or PDF</p>
                          </label>
                        </div>
                        <% if (award?.certificateUrl) { %>
                          <div class="mt-3 flex items-center gap-2 bg-white/5 p-2 rounded-lg border border-white/5">
                            <i data-lucide="file-text" class="w-4 h-4 text-teal-400"></i>
                            <span class="text-xs text-gray-400">Current:</span>
                            <a href="<%= award.certificateUrl %>" target="_blank"
                              class="text-teal-400 text-xs hover:underline truncate flex-1">View Current File</a>
                          </div>
                          <% } %>
                      </div>

                      <div id="urlInputContainer" class="hidden animate-in fade-in slide-in-from-top-2 duration-300">
                        <div class="relative group">
                          <div
                            class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-gray-500 group-focus-within:text-teal-400 transition-colors">
                            <i data-lucide="link" class="w-4 h-4"></i>
                          </div>
                          <input type="url" name="certificateUrl" value="<%= isCertUrl ? award.certificateUrl : '' %>"
                            placeholder="https://example.com/certificate.pdf"
                            class="pl-11 pr-4 py-3 bg-black/40 border border-white/10 rounded-xl focus:ring-2 focus:ring-teal-500/20 w-full outline-none transition-all text-sm">
                        </div>
                      </div>
                    </div>
                </div>
              </div>

              <script>
                function updateToggleUI(source) {
                  const fileContainer = document.getElementById('fileInputContainer');
                  const urlContainer = document.getElementById('urlInputContainer');
                  const highlight = document.getElementById('toggleHighlight');
                  const labels = document.querySelectorAll('[name="imageSource"] + span');

                  if (source === 'file') {
                    fileContainer.classList.remove('hidden');
                    urlContainer.classList.add('hidden');
                    if (highlight) {
                      highlight.style.left = '4px';
                      highlight.style.width = 'calc(50% - 4px)';
                    }
                    if (labels.length >= 2) {
                      labels[0].classList.add('text-black');
                      labels[0].classList.remove('text-gray-400');
                      labels[1].classList.add('text-gray-400');
                      labels[1].classList.remove('text-black');
                    }
                  } else {
                    fileContainer.classList.add('hidden');
                    urlContainer.classList.remove('hidden');
                    if (highlight) {
                      highlight.style.left = '50%';
                      highlight.style.width = 'calc(50% - 4px)';
                    }
                    if (labels.length >= 2) {
                      labels[1].classList.add('text-black');
                      labels[1].classList.remove('text-gray-400');
                      labels[0].classList.add('text-gray-400');
                      labels[0].classList.remove('text-black');
                    }
                  }
                }

                window.addEventListener('load', function () {
                  const checked = document.querySelector('input[name="imageSource"]:checked');
                  if (checked) updateToggleUI(checked.value);
                  else updateToggleUI('file');

                  if (window.lucide) window.lucide.createIcons();
                });
              </script>
              <div class="flex items-center gap-3">
                <a href="/admin/awards"
                  class="px-4 py-2 rounded-lg bg-white/5 hover:bg-white/10 text-gray-300">Batal</a>
                <button type="submit"
                  class="px-4 py-2 rounded-lg bg-teal-500 text-black font-semibold hover:bg-teal-600">Simpan</button>
              </div>
            </form>
    </main>
    <script>lucide.createIcons();</script>
</body>

</html>