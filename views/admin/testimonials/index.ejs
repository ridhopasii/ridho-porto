<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Testimonials - Admin Panel</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">
    <style type="text/tailwindcss">
        body { font-family: 'Plus Jakarta Sans', sans-serif; background: #0a0a0a; color: white; }
        .sidebar-link { @apply flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-white hover:bg-white/5 transition-all; }
        .sidebar-link.active { @apply bg-teal-500/10 text-teal-400; }
    </style>
</head>

<body class="flex min-h-screen">
    <%- include('../partials/sidebar', { path: '/admin/testimonials' }) %>

        <main class="flex-1 ml-64 p-8">
            <%- include('../partials/header', { title: 'Testimonials' }) %>

                <div class="flex justify-between items-center mb-8">
                    <h2 class="text-xl font-bold font-outfit">All Testimonials</h2>
                    <a href="/admin/testimonials/create"
                        class="bg-teal-500 hover:bg-teal-600 text-black font-bold py-2 px-4 rounded-lg flex items-center gap-2 transition-colors">
                        <i data-lucide="plus" class="w-4 h-4"></i>
                        Add Testimonial
                    </a>
                </div>

                <div class="bg-white/5 border border-white/10 rounded-2xl overflow-hidden">
                    <table class="w-full text-left">
                        <thead class="bg-black/20 border-b border-white/10">
                            <tr>
                                <th class="p-4 font-medium text-gray-400">Avatar</th>
                                <th class="p-4 font-medium text-gray-400">Name</th>
                                <th class="p-4 font-medium text-gray-400">Role</th>
                                <th class="p-4 font-medium text-gray-400">Message</th>
                                <th class="p-4 font-medium text-gray-400 text-right">Actions</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-white/5">
                            <% testimonials.forEach(testimonial=> { %>
                                <tr class="hover:bg-white/5 transition-colors">
                                    <td class="p-4">
                                        <% if (testimonial.avatarUrl) { %>
                                            <img src="<%= testimonial.avatarUrl %>" alt=""
                                                class="w-10 h-10 rounded-full object-cover bg-white/10">
                                            <% } else { %>
                                                <div
                                                    class="w-10 h-10 rounded-full bg-white/10 flex items-center justify-center text-gray-400">
                                                    <i data-lucide="user" class="w-5 h-5"></i>
                                                </div>
                                                <% } %>
                                    </td>
                                    <td class="p-4 bg-transparent text-white font-medium">
                                        <%= testimonial.name %>
                                    </td>
                                    <td class="p-4 text-gray-400">
                                        <%= testimonial.role %>
                                    </td>
                                    <td class="p-4 text-gray-400 max-w-md truncate">
                                        <%= testimonial.message %>
                                    </td>
                                    <td class="p-4 text-right">
                                        <div class="flex items-center justify-end gap-2">
                                            <a href="/admin/testimonials/edit/<%= testimonial.id %>"
                                                class="p-2 bg-blue-500/10 text-blue-400 rounded-lg hover:bg-blue-500/20 transition-colors">
                                                <i data-lucide="edit-2" class="w-4 h-4"></i>
                                            </a>
                                            <form action="/admin/testimonials/delete/<%= testimonial.id %>"
                                                method="POST" class="inline-block"
                                                onsubmit="return confirm('Are you sure you want to delete this testimonial?')">
                                                <button type="submit"
                                                    class="p-2 bg-red-500/10 text-red-400 rounded-lg hover:bg-red-500/20 transition-colors">
                                                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                                <% }) %>
                        </tbody>
                    </table>
                    <% if (testimonials.length===0) { %>
                        <div class="p-8 text-center text-gray-500">
                            No testimonials found. Start by creating one.
                        </div>
                        <% } %>
                </div>
        </main>
        <script>lucide.createIcons();</script>
</body>

</html>