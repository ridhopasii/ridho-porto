<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= organization ? 'Edit' : 'Add' %> Organization - Admin Panel</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/lucide@latest"></script>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style type="text/tailwindcss">
    body { font-family: 'Plus Jakarta Sans', sans-serif; background: #0a0a0a; color: white; }
    .sidebar-link { @apply flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-white hover:bg-white/5 transition-all; }
    .sidebar-link.active { @apply bg-teal-500/10 text-teal-400; }
    input, textarea, select { @apply w-full bg-black/20 border border-white/10 rounded-lg px-4 py-3 focus:outline-none focus:border-teal-500 transition-colors text-white; }
    label { @apply block text-sm text-gray-400 mb-1; }
  </style>
</head>
<body class="flex min-h-screen">
  <%- include('../partials/sidebar', { path: '/admin/organizations' }) %>
  <main class="flex-1 ml-64 p-8">
    <%- include('../partials/header', { title: organization ? 'Edit Organization' : 'Add Organization' }) %>
    <div class="max-w-2xl mx-auto">
      <div class="bg-white/5 border border-white/10 rounded-2xl p-8">
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="mb-4 p-3 rounded-lg bg-red-500/10 border border-red-500/20 text-red-400"><%= error %></div>
        <% } %>
        <form action="/admin/organizations<%= organization ? '/edit/' + organization.id : '/create' %>" method="POST" class="space-y-6">
          <div class="grid grid-cols-2 gap-4">
            <div>
              <label>Organization Name</label>
              <input type="text" name="name" value="<%= organization?.name || '' %>" required />
            </div>
            <div>
              <label>Role/Position</label>
              <input type="text" name="role" value="<%= organization?.role || '' %>" required />
            </div>
          </div>
          <div class="grid grid-cols-2 gap-4">
            <div>
              <label>Period</label>
              <input type="text" name="period" placeholder="2022 - Sekarang" value="<%= organization?.period || '' %>" required />
            </div>
            <div>
              <label>Order</label>
              <input type="number" name="order" min="0" value="<%= organization?.order ?? 0 %>" />
            </div>
          </div>
          <div>
            <label>Description</label>
            <textarea name="description" rows="4" placeholder="Deskripsi singkat"><%= organization?.description || '' %></textarea>
          </div>
          <div class="grid grid-cols-2 gap-4">
            <div>
              <label>Logo URL</label>
              <input type="text" name="logoUrl" value="<%= organization?.logoUrl || '' %>" />
            </div>
            <div>
              <label>Website</label>
              <input type="url" name="website" value="<%= organization?.website || '' %>" />
            </div>
          </div>
          <div class="flex gap-3">
            <button type="submit" class="flex-1 bg-teal-500 hover:bg-teal-400 text-black font-bold py-3 rounded-lg transition-colors">
              <%= organization ? 'Update Organization' : 'Add Organization' %>
            </button>
            <a href="/admin/organizations" class="flex-1 bg-white/10 hover:bg-white/20 text-white font-bold py-3 rounded-lg text-center transition-colors">Cancel</a>
          </div>
        </form>
      </div>
    </div>
  </main>
</body>
</html>
