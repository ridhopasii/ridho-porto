<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Setup - Hafidz Humaidi</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: 'Plus Jakarta Sans', sans-serif;
        background: #0a0a0a;
        color: white;
      }
    </style>
  </head>
  <body class="flex items-center justify-center min-h-screen">
    <div class="w-full max-w-md p-8 bg-white/5 border border-white/10 rounded-2xl backdrop-blur-xl">
      <h2 class="text-3xl font-bold font-outfit text-center mb-2">Setup Admin</h2>
      <p class="text-sm text-gray-400 text-center mb-6">
        Email login: <span class="text-gray-200"><%= email %></span>
      </p>

      <% if (locals.error) { %>
      <div class="bg-red-500/10 border border-red-500/20 text-red-500 p-3 rounded-lg mb-4 text-sm text-center">
        <%= error %>
      </div>
      <% } %>

      <% if (email !== allowedEmail) { %>
      <div class="bg-yellow-500/10 border border-yellow-500/20 text-yellow-300 p-3 rounded-lg mb-4 text-sm">
        Email ini belum diizinkan menjadi admin. Gunakan email: <span class="text-white"><%= allowedEmail %></span>
      </div>
      <% } %>

      <form action="/admin/setup" method="POST" class="space-y-4">
        <button
          type="submit"
          class="w-full bg-gradient-to-r from-teal-500 to-teal-600 hover:from-teal-400 hover:to-teal-500 text-black font-bold py-3 rounded-lg transition-all transform hover:scale-[1.02]"
        >
          Jadikan Admin
        </button>
      </form>

      <div class="mt-6 text-center">
        <a href="/admin/logout" class="text-sm text-gray-500 hover:text-white transition-colors">Keluar</a>
      </div>
    </div>
  </body>
</html>

