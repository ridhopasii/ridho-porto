<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= social ? 'Edit' : 'Add' %> Social Link - Admin Panel
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap"
        rel="stylesheet">
    <style type="text/tailwindcss">
        body { font-family: 'Plus Jakarta Sans', sans-serif; background: #0a0a0a; color: white; }
        .sidebar-link { @apply flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-white hover:bg-white/5 transition-all; }
        .sidebar-link.active { @apply bg-teal-500/10 text-teal-400; }
        input, textarea, select { @apply w-full bg-black/20 border border-white/10 rounded-lg px-4 py-3 focus:outline-none focus:border-teal-500 transition-colors text-white; }
        label { @apply block text-sm text-gray-400 mb-1; }
    </style>
</head>

<body class="flex min-h-screen">
    <%- include('../partials/sidebar', { path: '/admin/socials' }) %>

        <main class="flex-1 ml-64 p-8">
            <%- include('../partials/header', { title: social ? 'Edit Social Link' : 'Add Social Link' }) %>

                <div class="max-w-2xl mx-auto">
                    <div class="bg-white/5 border border-white/10 rounded-2xl p-8">
                        <form action="/admin/socials<%= social ? '/edit/' + social.id : '/create' %>" method="POST"
                            class="space-y-6">

                            <div>
                                <label>Platform Name</label>
                                <input type="text" name="platform" value="<%= social ? social.platform : '' %>" required
                                    placeholder="e.g. GitHub, LinkedIn">
                            </div>

                            <div>
                                <label>URL</label>
                                <input type="url" name="url" value="<%= social ? social.url : '' %>" required
                                    placeholder="https://...">
                            </div>

                            <div>
                                <label>Icon Name (Lucide)</label>
                                <div class="flex gap-4">
                                    <input type="text" name="icon" value="<%= social ? social.icon : '' %>" required
                                        placeholder="e.g. github, linkedin, twitter">
                                    <a href="https://lucide.dev/icons" target="_blank"
                                        class="px-4 py-3 bg-white/5 rounded-lg border border-white/10 hover:bg-white/10 transition-colors flex items-center justify-center">
                                        <i data-lucide="external-link" class="w-5 h-5 text-gray-400"></i>
                                    </a>
                                </div>
                                <p class="text-xs text-gray-500 mt-2">Check <a href="https://lucide.dev/icons"
                                        target="_blank" class="text-teal-400 hover:underline">Lucide Icons</a> for
                                    available icon names.</p>
                            </div>

                            <div class="flex gap-4 pt-4">
                                <button type="submit"
                                    class="flex-1 bg-teal-500 hover:bg-teal-600 text-black font-bold py-3 rounded-lg transition-colors">
                                    <%= social ? 'Update Link' : 'Add Link' %>
                                </button>
                                <a href="/admin/socials"
                                    class="flex-1 bg-white/10 hover:bg-white/20 text-white font-bold py-3 rounded-lg text-center transition-colors">
                                    Cancel
                                </a>
                            </div>
                        </form>
                    </div>
                </div>
        </main>
        <script>lucide.createIcons();</script>
</body>

</html>